{
  "name": "webkit-studio-client",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Klientske prostredi Webkit.Studio - Astro app pod webkit.studio/client",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "serve": "wrangler dev -c dist/server/wrangler.json --port 8788 --local --persist-to .wrangler-local",
    "db:migrate": "wrangler d1 execute DB -c dist/server/wrangler.json --local --persist-to .wrangler-local --file migrations/0001_init.sql",
    "db:seed": "node scripts/seed.mjs > .seed.sql && wrangler d1 execute DB -c dist/server/wrangler.json --local --persist-to .wrangler-local --file .seed.sql",
    "db:sql": "wrangler d1 execute DB -c dist/server/wrangler.json --local --persist-to .wrangler-local --command",
    "test": "node tests/auth.test.cjs && node tests/admin.test.cjs && node tests/comments.test.cjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.5",
    "@astrojs/react": "^6.0.4",
    "astro": "^7.2.9",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "relume-icons": "^1.4.0"
  },
  "devDependencies": {
    "@relume_io/relume-tailwind": "^1.3.0",
    "autoprefixer": "^10.5.4",
    "playwright-core": "^1.62.1",
    "postcss": "^8.5.26",
    "tailwindcss": "^3.4.19",
    "wrangler": "^4.127.0"
  }
}
